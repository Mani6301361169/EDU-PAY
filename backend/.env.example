# Copy this file to backend/.env and replace <db_password> with your MongoDB user password.
# Do not commit the .env file.
MONGODB_URI=mongodb+srv://mani_reddy:<db_password>@mycluster.b42xspp.mongodb.net/?appName=Mycluster
MONGODB_DB_NAME=college-fees-system
PORT=5000
# Optional: use this when your default DNS resolver blocks MongoDB SRV lookups.
MONGODB_DNS_SERVER=8.8.8.8
